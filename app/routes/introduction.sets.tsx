import LatexRenderer from "~/components/LatexRenderer";

const Sets = () => {
  return (
    <div>
      Sets
      <LatexRenderer latex="x_1" displayMode={false} />
    </div>
  );
};

export default Sets;
